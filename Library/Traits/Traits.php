<?php

/* Traits are PHP's way of adding extra functionality to a class.
 * Traits contain re-usable logic.
 * Why do we need a trait - can't we just extend another class?
 * What if there's logic from two classes that you'd like to re-use?
 */

